datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

/// Phase 1 scaffold model for Prisma Client generation without business logic.
model PlatformScaffold {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
}
